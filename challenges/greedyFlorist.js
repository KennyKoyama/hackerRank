// Complete the getMinimumCost function below.
function getMinimumCost(k, c) {
    let result
    if(c.length == k) return c.reduce((sum, curr) => sum + curr,0);
    

    return result
}

console.log(getMinimumCost(3,[1, 3, 5, 7, 9]))